import { Navbar } from "react-bootstrap"

export default function Navigationbar() {
    return(
        <Navbar bg="dark" variant="dark">
        <Navbar.Brand href="/">Let's go</Navbar.Brand>
      </Navbar>
    );

}